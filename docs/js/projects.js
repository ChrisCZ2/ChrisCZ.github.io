(async()=>{await requireUser();const modal=document.querySelector('#modal');document.querySelector('#newProject').onclick=()=>modal.classList.remove('hidden');document.querySelector('#close').onclick=()=>modal.classList.add('hidden');document.querySelector('#projectForm').onsubmit=async e=>{e.preventDefault();const body=Object.fromEntries(new FormData(e.target));await api('/api/projects',{method:'POST',body:JSON.stringify(body)});modal.classList.add('hidden');e.target.reset();load()};async function load(){const ps=await api('/api/projects'),g=document.querySelector('#projectGrid');g.innerHTML='';ps.forEach(p=>g.insertAdjacentHTML('beforeend',`<article class="project-card"><span class="swatch" style="background:${p.color}"></span><small>${p.client||'No client'}</small><h3>${esc(p.name)}</h3><p>${esc(p.description||'No description')}</p></article>`));if(!ps.length)g.innerHTML='<div class="panel"><p>No projects yet. Create your first project.</p></div>'}load()})();function esc(s){return String(s).replace(/[&<>\"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\\':'&#92;','"':'&quot;'}[c]))}
